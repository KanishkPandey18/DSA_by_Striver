#include <iostream>
using namespace std;

int main() {
    // int value = 10;
    // // Declare a pointer to an integer and store the address of 'value' in it
    // int* ptr = &value;

    // // Use the dereference operator (*) to access the data the pointer points to
    // int dereferenced_value = *ptr;

    // std::cout << "The value of the variable is: " << value << std::endl;
    // std::cout << "The address stored in the pointer is: " << ptr << std::endl;
    // std::cout << "The value at the address (dereferenced) is: " << dereferenced_value << std::endl;
    int sum = 10;
    cout << 15%7;
    return 0;
}
